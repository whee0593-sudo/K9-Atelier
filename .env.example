# Team password for privacy / preview mode (required on Vercel when site.privacyMode is true)
SITE_ACCESS_PASSWORD=change-me-to-a-strong-password

# Local default used in .env.local for preview: k9atelier-preview

# Set to "false" to temporarily disable privacy gating without editing business.json
# SITE_PRIVACY_MODE=false
